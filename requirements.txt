# Verified on Python 3.12.10 with streamlit 1.56.0 and pandas 3.0.2.
#
# Floors are set at the verified versions rather than guessed. The app calls
# st.dataframe(width="stretch") and st.bar_chart(stack=...), and .streamlit/
# config.toml uses theme.font URL syntax, showSidebarBorder, dataframeBorderColor,
# dataframeHeaderBackgroundColor and a [theme.sidebar] section -- all far newer
# than the streamlit>=1.30 floor this file used to claim. Upper bounds keep a
# surprise major release from breaking a deployed app nobody is watching.
streamlit>=1.56,<2
pandas>=3.0,<4
